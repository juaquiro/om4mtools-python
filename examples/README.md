# examples

Demo notebooks and scripts showing the core API in use. Not shipped in the
sdist/wheel, and excluded from packaging in `pyproject.toml`. Runnable on
Binder using the environment in `.binder/`.
